{
  "name": "shop-scheduler-pro",
  "version": "1.0.0",
  "description": "Work scheduler for HR and operations teams",
  "main": "server.js",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && npm run start",
    "start": "node server.js",
    "deploy": "gcloud run deploy shop-scheduler-pro --source . --allow-unauthenticated"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sql.js": "^1.11.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.1"
  }
}